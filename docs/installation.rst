============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install spacy-pattern-builder
    $ pip install spacy-pattern-builder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv spacy-pattern-builder
    $ pip install spacy-pattern-builder
